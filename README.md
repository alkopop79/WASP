# WASP

The idea of this project is to build a re-configurable, analogue signal processor. The code here is for re-configuring the target hardware. Since the original code won't compile on present versions of avrdude, I had to re-write it to Arduino. 
